{
  "name": "acs-azure-function-easy-auth",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start -- ",
    "test": "echo \"No tests yet...\"",
    "sample": "serve -s ./samples/webapp"
  },
  "dependencies": {
    "@azure/communication-identity": "^1.3.1",
    "@azure/data-tables": "^13.2.2",
    "@azure/functions": "^4.4.0"
  },
  "devDependencies": {
    "@types/node": "18.x",
    "azure-functions-core-tools": "^4.x",
    "serve": "^14.2.1",
    "typescript": "^4.0.0"
  },
  "main": "dist/src/functions/*.js"
}
